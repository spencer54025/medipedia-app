import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    textFieldWrapper: {
        marginTop: 20,
        marginBottom: 20
    },
    textField: {
        borderRadius: 20,
        height: 40,
        paddingLeft: 20,
        backgroundColor: "white"
    }
})